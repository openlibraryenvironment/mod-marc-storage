{
    "srvcId": "${artifactId}-${version}",
    "nodeId": "localhost",
    "descriptor": {
        "exec": "java -jar ../mod-courses/target/mod-marc-storage-fat.jar -Dhttp.port=%p embed_postgres=true"
    }
}
